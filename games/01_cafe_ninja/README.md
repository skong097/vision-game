# 카페 닌자 (Cafe Ninja)

> **PlayWait W3** — 세 번째 게임 / 첫 액션 게임
> **기술**: MediaPipe Hands (검지 끝 궤적 + 선분-원 충돌)
> **난이도**: 
> **상태**: 코드 구현 완료, 단위 테스트 105/105 통과 (실기 테스트 대기)

---

## 한 줄 소개

화면 아래에서 던져지는 **카페 메뉴**를 검지 끝으로 베는 액션 게임. 60초 안에 목표 점수 도달 시 승리, **폭탄을 베면 생명 -1**.

## 게임 규칙

- 1~3개씩 동시에 메뉴 객체가 포물선으로 던져짐
- 검지 끝(landmark 8) 궤적이 객체 영역(선분-원 거리 ≤ 반지름)을 통과하면 **슬라이스 성공**
- 한 스와이프로 **여러 개 동시 베기 → 콤보 보너스**
- 종료: 목표 점수 도달(승) / 생명 0(패) / 60초 초과(패)

### 객체와 점수

| 객체 | 점수 | 등장 빈도 |
|---|---|---|
| 아메리카노 | +10 | 35% |
| 라떼 | +15 | 25% |
| 카푸치노 | +20 | 15% |
| 케이크 | +25 | 10% (큰 sprite) |
| 크루아상 | +25 | 15% |
| 폭탄 | **생명 -1** | 난이도별 3~8% |

### 콤보 배수

| 동시 슬라이스 | 배수 |
|---|---|
| 1개 | ×1.0 |
| 2개 | ×1.5 (COMBO!) |
| 3개 | ×2.0 (GREAT COMBO!) |
| 4개+ | ×3.0 (WONDER COMBO!) |

폭탄은 콤보 카운트에 포함되지 않음 (메뉴만 카운트).

### 난이도

| 난이도 | 목표 점수 | spawn 간격 | 폭탄 비율 |
|---|---|---|---|
| Easy | 200점 | 1.2초 | 3% |
| Normal | 300점 | 0.9초 | 5% |
| Hard | 450점 | 0.6초 | 8% |

## 실행

```bash
cd ~/PlayWait
python -m games.01_cafe_ninja.src.game

# 단독 모듈 테스트
python -m games.01_cafe_ninja.src.finger_tracker      # 검지 트레일 시각화

# 단위 테스트
.venv/bin/pytest games/01_cafe_ninja/tests/ -v
```

## 조작

| 키 | 동작 |
|---|---|
| **1 / 2 / 3** | 난이도 선택 |
| **SPACE** | 게임 시작 |
| **R** | 재시작 (게임 종료 후) |
| **Q / ESC** | 종료 |
| **F** | 전체 화면 |
| **+ / =** | 화면 확대 / **- / _** 축소 / **0** 리셋 |

## 구조

```
01_cafe_ninja/
├── README.md                       # 본 문서
├── W3_카페닌자_기획서.md           # 상세 기획
├── src/
│   ├── game.py                     # 4페이즈 메인 게임
│   ├── finger_tracker.py           # 검지 끝 궤적 + 점-선분 거리 (순수 + MP 래퍼)
│   ├── falling_object.py           # 포물선 운동 데이터클래스 (순수)
│   ├── spawner.py                  # 난이도별 출현 (순수, 시드 격리)
│   ├── slice_judge.py              # 충돌 판정 + 콤보 점수 (순수)
│   ├── score_state.py              # 점수·생명·시간·종료 (순수)
│   ├── theme.py                    # PinkLAB 디자인 + W3 메뉴 색상 토큰
│   ├── ui_renderer.py              # W1·W2 + W3 컴포넌트(트레일/객체/HUD/게임오버)
│   └── sound_manager.py            # pygame 사운드 (W2 그대로)
├── assets/sounds/                  # 슬라이스/폭발 wav (현재 비어있음 — 콘솔 폴백)
└── tests/                          # pytest 105 cases
    ├── conftest.py
    ├── test_finger_tracker.py      # 25
    ├── test_falling_object.py      # 17
    ├── test_spawner.py             # 21
    ├── test_slice_judge.py         # 21
    └── test_score_state.py         # 21
```

## 페이즈

```
DIFFICULTY_SELECT → READY → PLAYING → GAME_OVER
                              ↻ (60초간 실시간 시뮬레이션)
```

PLAYING 한 프레임:
1. 검지 끝 좌표 추출 + 트레일 push
2. spawner가 dt 누적 후 객체 spawn
3. 모든 객체 step (포물선 운동)
4. 트레일이 **슬라이싱 중**(속도 ≥ 25 px/frame)이면 충돌 판정
5. 베인 객체에 score 적용 + 사운드 + 시각 효과 + 마킹 후 제거
6. 종료 조건 체크

## 디자인 (W1·W2 표준 유지)

- 카메라 640×480, `DISPLAY_SCALE=1.5`로 960×720 표시
- 검지 트레일: 그라데이션(과거 회색 → 최신 핑크), 슬라이싱 중 굵어짐
- 객체: 색상 원 + 영문 라벨 — sprite PNG로 추후 교체 가능
- 폭탄: 빨강 + 검정 테두리 + X 표시
- 콤보 팝업: ×1.5 핑크 / ×2.0 노랑 / ×3.0 빨강 ("WONDER COMBO!")
- 생명: /문자 (NanumGothic 기본 지원)

## Definition of Done

- [x] 검지 끝 궤적 추적 + 슬라이스 임계 판정
- [x] 포물선 운동 + 화면 밖 자동 제거
- [x] 메뉴 5종 + 폭탄 등장 + 비율 검증 (단위 테스트)
- [x] 콤보 ×1.5/2.0/3.0 정확히 적용
- [x] 60초 / 생명 0 / 목표 점수 종료 조건
- [x] 한글 UI (W1·W2 표준 준수)
- [x] **단위 테스트 105건 전부 통과**
- [ ] **실기 플레이 테스트 (Stephen 직접 수행)**
- [ ] 30fps 유지 확인
- [ ] 메뉴 sprite PNG 교체 (placeholder → 실제 이미지)
- [ ] (보류) YOLO 보너스 미션 — W7(좀비 피하기)에서 정식 도입
- [ ] (보류) 코어 추출 (`core/vision/mediapipe_engine.py`, `core/game_base/base_game.py`)

## W2 → W3 변경 핵심

| 항목 | W2 (스피드 카운터) | W3 (카페 닌자) |
|---|---|---|
| 입력 | 양손 손가락 합산 | 한 손 검지 끝 궤적 |
| 진행 | 라운드 기반 (출제) | 실시간 시뮬레이션 |
| 안정화 | StabilityBuffer 5/4 | **없음** (액션 게임은 즉시 반응) |
| 충돌/판정 | 정수 합계 동등 비교 | 점-선분 거리 ≤ 반지름 |
| 게임오버 사유 | 5연승 / 60s / 5오답 | 목표 점수 / 60s / 생명 0 |
| 객체 동시성 | 1개 (출제 숫자) | 다수 (1~3개 동시 + 콤보) |
