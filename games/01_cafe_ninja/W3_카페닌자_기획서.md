# 카페 닌자 (Cafe Ninja)

> **PlayWait W3 — 세 번째 게임 / 첫 액션 게임 / 첫 YOLO 도입**
> **개발 시작**: 2026-05-05
> **개발 기간**: 1주 (W3)
> **난이도**: (W1·W2 응용 + 신규 메커니즘)
> **기술**: MediaPipe Hands (검지 끝 궤적) + YOLO v8n (보너스 미션, Stretch)

---

## 게임 개요

**과일 닌자 스타일의 카페 메뉴 슬라이스 액션 게임.** 화면 아래에서 위로 던져지는 카페 메뉴 아이템을 손가락 궤적으로 베어내고, 폭탄(쓰레기)은 피한다. 60초 동안 목표 점수에 도달하면 승리.

### 핵심 가치 (B2B2C 관점)

| 이해관계자 | 효과 |
|---|---|
| **손님 (C)** | 짧고 강렬한 액션 — 대기시간을 즐겁게 보냄 |
| **매장 (B)** | **메뉴 노출 효과 직접 연결** — 게임 안에서 매장의 실제 메뉴 sprite가 화면을 채움. 대기하는 동안 손님이 자기도 모르게 "아메리카노 — 라떼 — 케이크" 시각 학습 |
| **플랫폼 (B)** | YOLO 보너스 미션으로 **실제 컵 인식** → 매장 머그컵 사용 손님에 추가 보상 → 친환경 캠페인/리유저블 컵 확산 데이터 |

### 핵심 특징

- **검지 끝 궤적 슬라이싱**: 손가락 끝(MediaPipe landmark 8)이 객체 영역을 빠르게 가로지르면 베기 성공
- **메뉴 5종 + 폭탄 1종**: 아메리카노 · 라떼 · 카푸치노 · 케이크 · 크루아상 · (폭탄=쓰레기 컵)
- **콤보 시스템**: 한 스와이프로 여러 객체 동시 베기 → 콤보 × 점수
- **60초 한 판**: 목표 점수(난이도별) 도달 = 승리, 실패 = 폭탄 3회/시간 초과
- **(Stretch) YOLO 보너스 미션**: 라운드 중간 "실제 컵을 보여주세요!" → 카메라에 컵을 비추면 +50점

---

## 게임 규칙

### 객체 종류와 점수

| 객체 | 점수 | 등장 빈도 | 비고 |
|---|---|---|---|
| 아메리카노 | +10 | 35% | 가장 흔함 |
| 라떼 | +15 | 25% | |
| 카푸치노 | +20 | 15% | |
| 케이크 | +25 | 10% | 큰 sprite, 천천히 떨어짐 |
| 크루아상 | +25 | 10% | |
| 폭탄 (쓰레기 컵) | **생명 -1** | 5% | 빨간 테두리 + 경고음 |

> 실제 sprite는 PinkLAB 스타일 일러스트(W3 작업 중 자체 제작 또는 placeholder).

### 콤보 보너스

- **2개 동시 베기**: 점수 ×1.5
- **3개 동시 베기**: 점수 ×2.0
- **4개+ 동시 베기**: 점수 ×3.0 + "WONDER COMBO!" 이펙트

### 승리/패배

- **승리**: 60초 안에 목표 점수 도달
- **패배 1**: 폭탄 3회 베기 (생명 0)
- **패배 2**: 60초 경과 후 목표 점수 미달

### 난이도 (3단계)

| 난이도 | 목표 점수 | 객체 출현 간격 | 폭탄 비율 |
|---|---|---|---|
| Easy | 200점 | 1.2초 | 3% |
| Normal | 300점 | 0.9초 | 5% |
| Hard | 450점 | 0.6초 | 8% |

---

## 기술 설계

### 1) 검지 끝 추적 + 스와이프 detection (W3 핵심)

```python
# MediaPipe Hands landmark 8 = 검지 끝
INDEX_TIP = 8

def get_index_tip_pixel(landmark, frame_w, frame_h):
    return (int(landmark[INDEX_TIP].x * frame_w),
            int(landmark[INDEX_TIP].y * frame_h))
```

**FingerTrail 클래스**:
- 최근 N(=10) 프레임의 검지 끝 좌표 deque
- 프레임 간 거리(픽셀)로 **속도(px/frame)** 계산
- `speed > SLICE_THRESHOLD`(예: 25px/frame ≈ 750px/s @ 30fps)일 때 "슬라이싱 중" 상태
- 시각화: 트레일 그라데이션 (최신=핑크 진함, 과거=투명)

**충돌 판정 (선분 vs 원)**:
- 직전 프레임 → 현재 프레임 검지 끝 = 선분 P1→P2
- 객체 = 중심 (cx, cy) + 반지름 r
- `point_to_segment_distance((cx,cy), P1, P2) <= r` → 슬라이스 성공

### 2) 떨어지는 객체 시뮬레이션

**FallingObject 클래스** (포물선 운동):
```python
self.x = x0
self.y = y0          # 시작은 화면 하단 밖
self.vx = vx0        # 좌우 약간의 초기 속도
self.vy = vy0        # 위로 솟구치는 초기 속도 (음수)
self.gravity = 0.5   # 매 프레임 vy += gravity
self.angular_vel = ω # 회전 (시각 효과)
```

매 프레임:
- `vy += gravity`
- `x += vx; y += vy`
- 화면 위로 안 사라지면 떨어짐 → 화면 밑으로 나가면 제거

### 3) 객체 스포너

```python
class Spawner:
    def __init__(self, difficulty):
        self.interval = SPAWN_INTERVAL[difficulty]
        self.bomb_ratio = BOMB_RATIO[difficulty]
        ...
    
    def update(self, dt) -> list[FallingObject]:
        """매 프레임 호출, 새로 spawn된 객체 리스트 반환"""
```

**spawn 다양성**:
- 단일 객체 또는 2~3개 동시 (콤보 유도)
- 화면 폭의 중앙 ±200px 범위에서 랜덤 x

### 4) YOLO 보너스 (Stretch — 1주 일정상 선택)

- 라운드 시작 30초 시점 1회 트리거
- 5초 카운트다운 동안 `ultralytics.YOLO("yolov8n.pt")`로 `cup`, `bottle` 클래스 detection
- 발견 시 +50점 보너스
- **FPS 영향 측정 후 도입**: YOLO 추론은 30fps의 ~50% 비용. 게임 중 매 프레임 X, 보너스 미션 5초 동안만 활성화하여 영향 최소화

---

## UI/UX

### 화면 레이아웃 (640×480)

```
┌──────────────────────────────────────────┐
│ 점수: 145 | 목표: 300 | | 42s │  상단 HUD
├──────────────────────────────────────────┤
│                                          │
│         [떨어지는 메뉴들]               │  게임 영역
│       ↗                       ↘          │
│                                       │
│                                       │  (폭탄=빨강)
│                                          │
│        ╱╱╱  손가락 트레일                 │
├──────────────────────────────────────────┤
│   COMBO ×2!  +30                         │  하단 피드백
└──────────────────────────────────────────┘
```

### 페이즈

```
DIFFICULTY_SELECT → READY → PLAYING → GAME_OVER
                              ↓
                      (선택) BONUS_MISSION (5s)
                              ↓
                          PLAYING 복귀
```

### 색상 (W1·W2 PinkLAB 톤 유지)

- 메뉴 객체: 따뜻한 톤 (베이지/브라운/핑크)
- 폭탄: 빨강 + 검정 테두리
- 손가락 트레일: PinkLAB 핑크 그라데이션
- 콤보 텍스트: 핑크 → 노랑 → 빨강 (콤보 강도)

### 사운드 (W1 6종 + 신규 2종)

| 사운드 | 트리거 |
|---|---|
| `slice.wav` (신규) | 슬라이스 성공 |
| `explosion.wav` (신규) | 폭탄 베기 |
| 기존 `victory/lose/click/countdown` | 페이즈 전환 |

---

## 파일 구조

```
games/01_cafe_ninja/
├── README.md                       # 게임 소개 (W3 마무리)
├── W3_카페닌자_기획서.md           # 본 문서
├── src/
│   ├── __init__.py
│   ├── game.py                     # 메인 게임
│   ├── finger_tracker.py           # 검지 끝 추적 + 스와이프 (Step 2)
│   ├── falling_object.py           # 객체 운동 (Step 3)
│   ├── spawner.py                  # 객체 출현 (Step 3)
│   ├── slice_judge.py              # 충돌 판정 (Step 4)
│   ├── score_state.py              # 점수/생명/타이머 (Step 4)
│   ├── yolo_bonus.py               # (Stretch) YOLO 컵 인식
│   ├── theme.py                    # W1·W2 디자인 토큰 + 신규
│   ├── ui_renderer.py              # W1·W2 + 슬라이스 트레일 컴포넌트
│   └── sound_manager.py            # W1 + slice/explosion 추가
├── assets/
│   ├── sprites/                    # 메뉴 아이템 PNG (W3 작업 중 제작)
│   │   ├── americano.png
│   │   ├── latte.png
│   │   ├── cappuccino.png
│   │   ├── cake.png
│   │   ├── croissant.png
│   │   └── bomb.png
│   └── sounds/
│       ├── (W1 6종 재활용)
│       ├── slice.wav (신규)
│       └── explosion.wav (신규)
└── tests/
    ├── conftest.py                 # W2 패턴 그대로
    ├── test_falling_object.py      # 포물선 운동 검증
    ├── test_slice_judge.py         # 점-선분 거리 충돌
    └── test_score_state.py         # 점수/생명/종료조건
```

---

## W1·W2 → W3 재활용 모듈

| 모듈 | 재활용도 | 비고 |
|---|---|---|
| `theme.py` | 90% | 메뉴 sprite 색상 토큰 추가 |
| `ui_renderer.py` | 80% | 슬라이스 트레일 + 콤보 이펙트 신규 |
| `sound_manager.py` | 100% | 사운드 파일만 추가 |
| `StabilityBuffer` | | 액션 게임이라 안정화 X (즉시 반응) |
| game.py 페이즈 구조 | 70% | DIFFICULTY_SELECT/READY/GAME_OVER 동일, PLAYING은 신규 |
| 안전 종료 패턴 | 100% | try-finally + signal + atexit |

---

## 코어 모듈 추출 (W3 후반)

W1·W2 두 케이스의 공통점이 명확해진 시점이라 다음 모듈을 `core/`로 이동/추출:

| 후보 | 위치 | 추출 시점 | 근거 |
|---|---|---|---|
| `MediaPipeHandsEngine` | `core/vision/mediapipe_engine.py` | W3 Step 2 후 | W1·W2·W3 모두 손 인식 — 카메라 초기화/MP 설정/거울 보정/안전 cleanup 공통 |
| `BaseGame` | `core/game_base/base_game.py` | W3 Step 5 통합 시 | DIFFICULTY_SELECT → ... → GAME_OVER 페이즈 패턴 + 안전 종료 + 화면 크기 키바인딩 |
| `OverlayRenderer` (PIL 배치 한글) | `core/ui/overlay_renderer.py` | W3 Step 5 | begin_frame/draw_text_korean/flush_text + 폰트 캐시 |
| `SoundManager` | `core/game_base/sound_manager.py` | W3 Step 5 | 이미 모듈화 완료, 위치만 이동 |

**주의**: 추출 시 W1·W2 게임이 깨지지 않게 import 경로 호환성 유지 (양쪽에서 import 가능하도록 src/ 폴더에 alias). W3 작업 중 점진 이동.

---

## W3 일정 (이번 주)

| 일자 | 작업 | 산출물 |
|---|---|---|
| **월 (5/5)** | Step 1: 기획서 | `W3_카페닌자_기획서.md` (본 문서) |
| **월~화** | Step 2: 검지 추적 + 스와이프 | `finger_tracker.py` |
| **화~수** | Step 3: 떨어지는 객체 + 스포너 | `falling_object.py`, `spawner.py` + 단위 테스트 |
| **수~목** | Step 4: 충돌 판정 + 점수 | `slice_judge.py`, `score_state.py` + 단위 테스트 |
| **목** | Step 5: 게임 통합 + UI + 코어 추출 | `game.py` + `core/` 일부 |
| **금** | Step 6 (Stretch) + 마무리 | YOLO 보너스 / `README.md` / `W3_개발로그_*.md` |

---

## Definition of Done

- [ ] 검지 끝 궤적 추적 정확도 95%+ (단독 테스트로 검증)
- [ ] 슬라이스 판정이 시각적으로 자연스러움 (실기 테스트)
- [ ] 메뉴 sprite 5종 + 폭탄 정상 출현 + 포물선 운동
- [ ] 콤보 보너스 (×1.5, ×2.0, ×3.0) 정확히 적용
- [ ] 60초 / 생명 0 / 목표 점수 종료 조건 정확
- [ ] 30fps 유지 (YOLO 미사용 시)
- [ ] 단위 테스트: 충돌 판정, 객체 운동, 점수 상태
- [ ] 한글 UI 정상 (W1·W2 표준 준수)
- [ ] 코어 모듈 1개 이상 `core/`로 추출 (MediaPipeEngine 우선)
- [ ] (Stretch) YOLO 보너스 미션 동작 + FPS 영향 측정

---

## W3에서 새로 학습할 것

1. **액션 게임 메커니즘** (W1 라운드제, W2 시간제, W3 실시간)
2. **선분-원 충돌 판정** (점-선분 거리 공식)
3. **포물선 운동 시뮬레이션**
4. **YOLO 통합** (`ultralytics`) 첫 실전 도입
5. **FPS 영향 측정** 방법론 (vision 모델 추가 시 표준 절차)
6. **공통 코어 추출** TDD 방식 (W1·W2 합의 시점)

---

**문서 버전**: v1.0
**최종 수정**: 2026-05-05
