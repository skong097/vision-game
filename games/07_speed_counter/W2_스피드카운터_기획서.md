# 스피드 카운터 (Speed Counter)

> **W2 게임 - PlayWait 두 번째 게임**
> **개발 시작**: 2026-04-30 (W2 시작)
> **개발 기간**: 1주 (W2)
> **난이도**: (W1 응용)

---

## 게임 개요

화면에 등장하는 숫자(1~10)를 **두 손 손가락**으로 빠르게 표현하는 콤보 게임. 정답을 맞힐수록 제한시간이 짧아지며, **5연속 정답 도달 시 승리**.

### 핵심 특징
- **두 손 합산** 으로 1~10 표현 (예: 7 = 5 + 2)
- **가속 시스템**: 콤보가 쌓일수록 시간 단축 (3초 → 1.2초)
- **콤보 게임**: 한 번 틀리면 콤보 리셋
- **짧고 강렬**: 평균 13~15초로 5연속 도달 가능

---

## 게임 규칙

### 숫자 표현 방법

**두 손 손가락 합산**: 양손을 모두 카메라에 보여주고, 펴진 손가락 개수의 합으로 숫자 표현.

| 숫자 | 표현 예시 |
|------|----------|
| 1 | 한 손 1개 |
| 2 | 한 손 2개 또는 양손 1+1 |
| 5 | 한 손 5개 (전부 펴기) |
| 7 | 5 + 2 또는 4 + 3 |
| 10 | 양손 5 + 5 (전부 펴기) |

**중요**: 정확한 손 모양보다 **합계만** 맞으면 정답. 표현 자유도 높음.

### 게임 진행

```
[게임 시작]
   ↓
[난이도 선택]
   ↓
[라운드 시작 - 콤보 0]
   ↓
┌─→ [숫자 출제 (1~10 랜덤)]
│       ↓
│  [3초 카운트다운 (또는 가속)]
│       ↓
│  [손가락 안정 평가 (1초)]
│       ↓
│  [정답 판정]
│   ├─ 정답 → 콤보 +1, 다음 문제
│   └─ 오답/시간초과 → 콤보 리셋, 오답 카운트 +1
│
└─< [5연속 도달?]  → YES → 승리
    [60초 초과?]   → YES → 패배
    [오답 5회?]    → YES → 패배
```

### 가속 시스템 (시간 단축)

콤보에 따라 답 확정 시간이 단축됩니다.

| 콤보 | 제한 시간 | 비고 |
|------|----------|------|
| 0~2 | **3.0초** | 워밍업 |
| 3~4 | **2.5초** | 가속 시작 |
| 5+ | **2.0초** | 즉시 승리 |

**참고**: 5연속 시 즉시 게임 종료라 5콤보 이후 시간은 사실상 사용 X. 디자인적으로는 더 어려운 변형 모드(10콤보 이상)에 활용 가능.

### 손 안정성 평가

답 확정은 다음 조건이 모두 만족되어야 함.

1. **합계가 출제 숫자와 일치**
2. **0.5초 이상 같은 합계 유지** (안정 평가)

흔들림이 심하면 안정 평가 통과 못 함 → 다시 시도.

---

## 승리/패배 조건

### 승리
- **5연속 정답 도달** → 즉시 게임 종료 + 쿠폰 발급

### 패배
- **60초 경과** → 시간 초과
- **누적 오답 5회** → 너무 많은 실수

---

## AI 난이도 (3단계) - W2는 시간 가속 차이로 구현

### Easy (쉬움) - 어린이/입문자
- **워밍업**: 4초 → 가속 시 3.5초 → 5+ 콤보 3.0초
- **목표 승률**: 70%

### Normal (보통) - 일반 손님 기본값
- **워밍업**: 3.0초 → 2.5초 → 2.0초
- **목표 승률**: 50%

### Hard (어려움) - 고수
- **워밍업**: 2.0초 → 1.7초 → 1.4초
- **목표 승률**: 30%

---

## UI/UX 설계

### 화면 레이아웃 (640x480)

```
┌─────────────────────────────────────────┐
│  콤보: 3  |  오답: 1/5  |  시간: 28s    │  상단 (점수바)
├─────────────────────────────────────────┤
│                                         │
│              7                          │  중앙 (큰 숫자)
│         숫자를 만드세요!                  │
│                                         │
├─────────────────────────────────────────┤
│       남은 시간: 2.5초                    │  타이머 바
│       [████████░░░░░░░░]                 │
├─────────────────────────────────────────┤
│        현재 손가락: 5 + 2 = 7          │  사용자 영역
│       (양손을 카메라에 보여주세요)         │
└─────────────────────────────────────────┘
```

### 페이즈 구성 (W1 패턴 재활용)

```
DIFFICULTY_SELECT  난이도 선택
        ↓
READY              "스페이스로 시작!"
        ↓
QUESTION          [숫자 출제 + 카운트다운 시작]
        ↓
ANSWER_CHECK      [손 모양 확인 + 안정 평가]
        ↓
ROUND_RESULT      [정답/오답 표시]
        ├─ 5연속 → GAME_OVER (승리)
        ├─ 60초/오답 5회 → GAME_OVER (패배)
        └─ 그 외 → QUESTION 으로 복귀
```

### 색상 (W1 동일 - PinkLAB 톤)
- 정답: 초록 (`COLOR_WIN`)
- 오답: 빨강 (`COLOR_LOSE`)
- 타이머 위험 (1초 미만): 빨강 깜빡임
- 콤보 표시: 핑크 (PinkLAB 메인)

---

## 기술 설계

### 핵심 모듈

```
07_speed_counter/src/
├── hand_counter.py        # 양손 손가락 합산 (W1 분류기 응용)
├── question_gen.py        # 1~10 숫자 출제기
├── score_tracker.py       # 콤보/오답/시간 관리
├── game.py                # 메인 게임 (W1 game.py 90% 재활용)
└── (W1 공유 모듈)
    - theme.py
    - ui_renderer.py
    - sound_manager.py
```

### 손가락 합산 알고리즘 (W1 응용)

```python
def count_fingers_total(results) -> int | None:
    """양손 손가락 개수 합산
    
    Returns:
        합계 (0~10) 또는 None (인식 실패)
    """
    if not results.multi_hand_landmarks:
        return None
    
    total = 0
    for hand_landmarks, handedness in zip(
        results.multi_hand_landmarks,
        results.multi_handedness
    ):
        # W1 의 get_finger_states() 재활용
        states = get_finger_states(hand_landmarks.landmark, hand_label)
        total += sum(states)  # 펴진 손가락 개수
    
    return total
```

**W1 대비 변경**: 한 손 → 양손 처리, `max_num_hands=2` 로 변경.

### 안정성 평가 (W1 StabilityBuffer 응용)

```python
# 윈도우 크기는 더 짧게 (반응 속도 ↑)
self.stability = StabilityBuffer(window_size=5, threshold=4)
```

W1 (10/7) 대비 W2 는 (5/4) - 더 빠른 확정.

### 가속 시간 결정

```python
def get_time_limit(combo: int, difficulty: str) -> float:
    """콤보와 난이도에 따른 제한 시간"""
    base = {
        "easy": 4.0,
        "normal": 3.0,
        "hard": 2.0,
    }[difficulty]
    
    if combo < 3:
        return base
    elif combo < 5:
        return base * 0.83  # 약 -0.5초
    else:
        return base * 0.67  # 약 -1.0초
```

---

## 파일 구조

```
07_speed_counter/
├── README.md              # 본 문서
├── src/
│   ├── __init__.py
│   ├── game.py            # 메인 게임
│   ├── hand_counter.py    # 양손 손가락 합산
│   ├── question_gen.py    # 숫자 출제
│   └── score_tracker.py   # 점수/콤보 관리
├── assets/
│   └── sounds/            # W1 사운드 재활용 가능
└── tests/
```

---

## W2 개발 일정

W1 인프라 재활용으로 빠르게 진행.

| 일자 | 작업 | 산출물 |
|------|------|--------|
| **월 (4/30)** | Step 1: 기획서 | `README.md` |
| **월** | Step 2: 양손 손가락 합산기 | `hand_counter.py` |
| **화** | Step 3: 게임 로직 (출제/점수/콤보) | `question_gen.py`, `score_tracker.py` |
| **수** | Step 4: UI 통합 (W1 재활용) | `game.py` |
| **목** | Step 5: 사운드 + 테스트 | 통합 테스트 |
| **금** | Step 6: 일별 .md 로그 + W2 회고 | `2026-05-04.md` |

**W1 대비 단축 예상**: 약 60% (인프라 재활용 효과)

---

## 완료 기준 (Definition of Done)

- [ ] 양손 손가락 합산 1~10 모두 정상 인식 (양 손 상관없이)
- [ ] 가속 시간 단축이 콤보별로 정확히 작동
- [ ] 5연속 정답 시 즉시 게임 종료
- [ ] 60초 / 오답 5회 시 게임 종료
- [ ] 한글 UI 정상 표시 (W1 인프라 재활용)
- [ ] 사운드 정상 재생 (콤보 사운드 추가 검토)
- [ ] 단위 테스트 (question_gen, score_tracker)
- [ ] 30fps 유지

---

## W1 → W2 재활용 모듈

| 모듈 | 재활용 방식 |
|------|-------------|
| `theme.py` | 100% 재활용 (디자인 토큰 동일) |
| `ui_renderer.py` | 100% 재활용 (배치 렌더링, 한글 PIL) |
| `sound_manager.py` | 100% 재활용 (같은 사운드 6종 사용 가능) |
| `judge.py` | 사용 안 함 (W2는 단순 정답 판정) |
| `ai_player.py` | 사용 안 함 (W2는 AI 상대 없음) |
| `game.py` 의 페이즈 구조 | 70% 재활용 (페이즈 이름만 변경) |
| `game.py` 의 cleanup 패턴 | 100% 재활용 (안전 종료) |
| `StabilityBuffer` | 윈도우 크기만 조정 |

---

## W2에서 새로 학습할 것

1. **양손 처리** (W1 한 손 → W2 두 손)
2. **타이머 기반 게임 진행** (W1 라운드제 → W2 시간제)
3. **콤보 시스템** (W2 신규)
4. **시간 가속 메커니즘** (W2 신규)

---

**문서 버전**: v1.0
**최종 수정**: 2026-04-30
